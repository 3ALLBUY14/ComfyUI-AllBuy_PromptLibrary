[pytest]
# rootdir 钉在本目录：插件根带 __init__.py（ComfyUI 自定义节点要求）且目录名含
# 连字符，pytest 9 默认会把插件根收集为 Package 并导入其 __init__.py（包内相对
# 导入离开 ComfyUI 运行时必然 ImportError，全部用例收集失败）。本文件让 rootdir
# = tests/，插件根退出收集树；tests/ 无 __init__.py，走普通 Dir 收集。
# 调用：在插件根 `pytest tests/`，或在 tests/ 里直接 `pytest`。
